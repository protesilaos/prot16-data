Prot16 colour values
====================

This repo contains `.yml` files with the colour values of each Prot16 theme.

To view the more comprehensive source files (which also include colour mapping and terminal values) refer to the Prot16 Generator (in particular the `schemes` directory: https://github.com/protesilaos/prot16-generator
